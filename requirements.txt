# Version ranges, not hard pins, so a fresh install works on any modern Python.
# numpy==1.26.4 shipped no wheels for Python 3.13+, so pip tried to build it
# from source and failed on machines with a current python.org install.
# Lower bounds keep the versions we tested; upper bounds guard the next major.
fastapi>=0.115,<1
uvicorn>=0.30,<1
yfinance>=0.2.65,<1
pandas>=2.2,<3
numpy>=1.26,<3
httpx>=0.27
